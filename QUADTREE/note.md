네 개 구역의 길이를 각각 구해서 뒤집어 주는 방법이 있고, 
책과 같이 무조건 반복자를 하나씩 증가시키면, 자연스럽게 구역이 나눠지는 방법이 있다.
- quadtree.begin()을 바로 함수로 넣는 것은 임시 변수를 넘겨주는 것이기 때문에 문법 에러가 난다.
- 즉, identifier가 존재하는 lvalue만이 non-const reference(&)가 가능하다.
- 이름 없는 value는 rvalue라고 부른다.