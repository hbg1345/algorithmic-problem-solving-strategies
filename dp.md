# 최적화

1. 완전 탐색
2. 남은 선택에 대한 답만 반환하도록
3. 이전의 선택과 관련된 정보는 최대한 줄이기

# 경우의 수 및 확률
1. 완전 탐색 - 모든 경우가 선택지에 포함되면서 경우 하나는 최대 하나의 선택지에 포함됨
2. 이전 조각에서 결정한 요소들을 줄이거나 제거

# 테크닉
## 답 구성하기
1. 최적해를 만들었던 선택을 별도의 배열에 저장
2. 별도의 재귀함수를 통해 선택지를 추적

# 조합 게임
1. 대칭/ 비대칭 게임에서 승패 예측
2. 게임판을 bit mask로 표현

# 반복적 동적 계획법
1. 슬라이딩 윈도우 기법 사용 가능
2. $C[i, j] = \sum_{k=0}^{n-1} W[j, k] * C[i-1, k]$ -> i가 가중치에 관여하지 않으므로 W행렬로 변경 가능
